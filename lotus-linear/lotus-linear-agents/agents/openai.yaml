interface:
  display_name: "Lotus Linear Agents"
  short_description: "Route Linear-backed Lotus work."
  default_prompt: "Use $lotus-linear-agents to route this repository's Linear-backed Lotus workflow."

dependencies:
  tools:
    - type: "mcp"
      value: "linear"
      description: "Linear connector for project, issue, document, profile, and comment operations."

policy:
  allow_implicit_invocation: true
